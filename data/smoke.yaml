# train and val datasets (image directory or *.txt file with image paths)
train: data/ImageSets/train.txt
val: data/ImageSets/val.txt
test: data/ImageSets/test.txt

# number of classes
nc: 1

# class names
names: ['smoke']
